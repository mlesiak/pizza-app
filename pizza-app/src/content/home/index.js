import { PageWrapper } from "../../common/page-wrapper";
import Typography from "@mui/material/Typography";

export const Home = () => {
  return (
    <PageWrapper title="Home">
      <Typography>Welcome to our Pizzeria!</Typography>
    </PageWrapper>
  );
};
